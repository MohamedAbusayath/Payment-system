apiVersion: v1
kind: PersistentVolume

metadata:
  name: payment-pv

spec:
  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /run/desktop/mnt/host/c/k8s-data/payment-data