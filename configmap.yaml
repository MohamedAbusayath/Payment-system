apiVersion: v1
kind: ConfigMap

metadata:
  name: payment-config

data:
  SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:3306/doodleblue
  SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  SERVER_PORT: "8080"